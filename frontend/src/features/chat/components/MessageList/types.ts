export interface IMessagesType {
  author: string;
  createdAt: string;
  message: string;
  messageId: string;
}

export interface IUserStatusMessagesType {
  userName: string;
  id: string;
  status: string;
  roomName: string;
}
